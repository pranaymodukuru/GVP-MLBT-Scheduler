[project]
name = "mlbt-scheduler"
version = "0.1.0"
description = "GVP MLBT School Timetable Auto-Scheduler"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv",
]

[tool.uv]
dev-dependencies = []
